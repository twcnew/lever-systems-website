/** Confetti Claude — timing from ayotomcs.me/claude-mascot (Codrops article). */
export const FRAME_DURATION = 0.125;

/** Confetti burst vertical offsets per sprite frame. */
export const PARTICLE_Y_OFFSETS = [-65, -72, -76, -70, -58, -42, -22, 0] as const;

export const CHARACTER_FRAME_COUNT = 8;
export const PARTICLE_FRAME_COUNT = 8;
